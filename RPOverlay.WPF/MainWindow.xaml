<Window x:Class="RPOverlay.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:RPOverlay.Core.Models;assembly=RPOverlay.Core"
        mc:Ignorable="d"
        Title="Path Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Visibility="Visible"
        WindowStartupLocation="Manual"
        MinWidth="280"
        MinHeight="300"
        Width="320"
        Height="500">
    <Grid Background="Transparent">
        <Border HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="24"
                Padding="16"
                CornerRadius="12"
                Background="#CC1E1E1E">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Draggable header -->
                <Border Grid.Row="0"
                        Background="#22FFFFFF" 
                        Padding="8,4"
                        Margin="-16,-16,-16,8"
                        CornerRadius="12,12,0,0"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                        Cursor="SizeAll">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="RP Overlay"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       FontSize="18" />
                            <TextBlock Text="Sjukhus-overlay (Dra för att flytta)"
                                       Margin="0,2,0,0"
                                       Foreground="#FFD0D0D0"
                                       FontSize="10" />
                        </StackPanel>
                        
                        <Button Grid.Column="1"
                                Content="✕"
                                Click="HideButton_Click"
                                VerticalAlignment="Center"
                                Width="30"
                                Height="30"
                                Background="Transparent"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="18"
                                Cursor="Hand"
                                ToolTip="Dölj overlay (F9)"/>
                    </Grid>
                </Border>
                
                <!-- Hotkey hint -->
                <Border Grid.Row="1"
                        Background="#33000000"
                        Padding="8"
                        CornerRadius="6"
                        Margin="0,0,0,8">
                    <TextBlock Text="{Binding HotkeyHint, StringFormat=Toggle: {0}}"
                               Foreground="#FFB0B0B0"
                               FontSize="12" />
                </Border>
                
                <!-- Buttons -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Hidden"
                              MaxHeight="200">
                    <ItemsControl ItemsSource="{Binding Buttons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:OverlayButton}">
                                <Button Content="{Binding Label}"
                                        Tag="{Binding}"
                                        Click="PresetButton_Click"
                                        Margin="0,4,0,0"
                                        HorizontalContentAlignment="Left"
                                        FontSize="14"
                                        Padding="12,10"
                                        Background="#FF2F9DFF"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Notes section with tabs -->
                <Grid Grid.Row="3" Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Tab Headers -->
                    <TabControl Grid.Row="0" Grid.RowSpan="2"
                                x:Name="NotesTabControl"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border Name="Border"
                                                    Background="#FF1C1C1C"
                                                    BorderBrush="#FF2F9DFF"
                                                    BorderThickness="1,1,1,0"
                                                    CornerRadius="4,4,0,0"
                                                    Padding="8,4"
                                                    Margin="0,0,4,0">
                                                <ContentPresenter ContentSource="Header"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Center">
                                                    <ContentPresenter.Resources>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="FontSize" Value="11"/>
                                                        </Style>
                                                    </ContentPresenter.Resources>
                                                </ContentPresenter>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#FF2F9DFF"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="False">
                                                    <Setter TargetName="Border" Property="Background" Value="#FF2A2A2A"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="White"/>
                            </Style>
                        </TabControl.Resources>
                    </TabControl>
                    
                    <!-- Resize grip - higher z-index to ensure it's clickable -->
                    <Canvas Grid.Row="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Width="20"
                            Height="20"
                            Margin="2,0,0,2"
                            Background="Transparent"
                            Panel.ZIndex="1000">
                        <Thumb x:Name="ResizeThumb"
                               Width="20"
                               Height="20"
                               Cursor="SizeNWSE"
                               DragDelta="ResizeThumb_DragDelta">
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Grid Background="#22000000">
                                        <!-- Grip dots istället för linjer - modernare look -->
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="14" Canvas.Top="14"/>
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="10" Canvas.Top="14"/>
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="14" Canvas.Top="10"/>
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="6" Canvas.Top="14"/>
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="10" Canvas.Top="10"/>
                                        <Ellipse Width="3" Height="3" Fill="#FF2F9DFF" Canvas.Left="14" Canvas.Top="6"/>
                                    </Grid>
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Canvas>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
